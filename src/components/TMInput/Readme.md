### TM Input:

```
const id = 'id'

const hintText = 'hintText'

const plateHolder = 'plateHolder'

<TMInput
  id={id}
  hintText={hintText}
  plateHolder={plateHolder}
/>

```
### Example Codes:

```javascript

import {TMInput} from 'react-ticket-ui';

const id = 'id'

const hintText = 'hintText'

const plateHolder = 'plateHolder'

<TMInput
  id={id}
  hintText={hintText}
  plateHolder={plateHolder}
/>


```
### Options:

option name | Describe
------------- | ------------- 
id | id of component
hintText | hintText
plateHolder | plateHolder
color | color of text input

